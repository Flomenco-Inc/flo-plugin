name: agent guidance sync

on:
  pull_request:
    branches: [main]
  merge_group:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  agent-guidance:
    name: check / agent-guidance
    uses: Flomenco-Inc/flo-docs/.github/workflows/agent-guidance-sync.yml@bde19b837699de8d6a11e791d5a5d197fd2c1910 # v1
